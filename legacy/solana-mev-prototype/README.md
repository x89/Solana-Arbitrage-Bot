# Archived `solana-mev` prototype

These files were never part of the maintained Cargo target. They describe an
obsolete wallet-loading MEV bot concept and are retained only for historical
reference.

The supported observation-only implementation is in
[`../../solana-mev/src`](../../solana-mev/src).
